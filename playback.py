old_string = input("say something ")
#prompts the user to say something and collects it as a string
new_string = old_string.replace(" ", "...")
#uses the old string to mke the new string
#new string replaces spaces with '...'
print(new_string)
#this function prints the new string with the '...' replacing spaces